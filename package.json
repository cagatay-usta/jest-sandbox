{
  "name": "jest-sandbox",
  "version": "1.0.0",
  "description": "a repo to try and test jest features",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
